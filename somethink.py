            #for self.file_name in self.coped_file_list: #TODO: Упростить отображение скопированных файлов.
                #name = self.file_name.partition('_copy_')[-3]
                #exe_and_nums = self.file_name.partition('_copy_')[-1]
                #num = exe_and_nums.partition('.')[-3]
                #exe = exe_and_nums.partition('.')[-1]
                #if int(num) == 1:
                    #index_dict[name] = [int(num)]
                    #exe_list.append(exe)
                #else:
                    #index_dict[name].append(int(num))
            #self.file_list.clear()
            #y = 0
            #for x in index_dict:
                #self.file_name = x + '_copy_' + str(max(index_dict[x])) + '.' + exe_list[y]
                #self.file_list.append(self.file_name)
                #y += 1